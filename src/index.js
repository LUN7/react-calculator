import React from "react";
import ReactDOM from "react-dom";
import Cal from "./cal";

import "./styles.css";

const rootElement = document.getElementById("Cal");
ReactDOM.render(<Cal />, rootElement);
